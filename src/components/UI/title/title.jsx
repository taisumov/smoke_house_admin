import React from 'react';
import classes from './title.module.css';

const Mtitle = (props) => {
    return (
       <div className={classes.mtitle} {...props}></div>
    );
};

export default Mtitle;